'use client';

import { createManifestWidget } from '../../mount/create-widget-component';
import { extraQuestionsManifest } from './manifest';

/** extra-questions on the host React, from its full manifest. */
export default createManifestWidget(extraQuestionsManifest);
